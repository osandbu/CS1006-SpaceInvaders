package alien.entity;

/**
 * The Movable interface is used to specify if an object can be moved around on
 * the screen.
 * 
 * @author Ole
 */
public interface Movable {
	/**
	 * Specifies the movement behaviour of an object.
	 */
	public void move();
}
